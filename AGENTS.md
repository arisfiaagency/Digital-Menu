# Agent Notes

- Do not start local development servers for this project unless the user explicitly asks for it.
- A local server may already be running in another terminal. Reuse the existing server when possible, and only verify it with non-invasive checks if needed.
- Do not stop or restart the existing server unless the user explicitly asks for that action.
